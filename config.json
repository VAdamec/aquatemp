{
  "name": "Warmlink Add-On",
  "version": "1.0.0",
  "slug": "ha-warmlink-addon",
  "description": "Set of script which, via MQTT, allow Home Assistant to control and show metrics from Warmlink heatpumps",
  "url": "https://github.com/VAdamec/aquatemp",
  "arch": ["amd64", "i386"],
  "startup": "services",
  "boot": "auto",
  "init": false,
  "options": {
    "warmlink_username": "your_username",
    "warmlink_password": "",
    "mqtt_username": "mqtt_username",
    "mqtt_password": ""
  },
  "schema": {
    "warmlink_username": "str",
    "warmlink_password": "str",
    "mqtt_username": "str",
    "mqtt_password": "str"
  }
}
